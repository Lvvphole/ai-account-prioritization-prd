# /prd-create

Purpose: create or update a modular PRD file.

Required inputs:

- feature name
- source docs
- acceptance criteria
- eval requirement
- owner role

Output:

- one `.md` file in `docs/features/`
- one task map item in `tasks/mcp_task_map.json`
