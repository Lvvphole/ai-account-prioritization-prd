# Optional Vercel Python Runtime Support

This folder is optional. It is not the primary agent orchestration layer.

Use only for:

- scoring preview service.
- batch data quality checks.
- eval fixture generation.

Do not move Vercel Eve orchestration into Python.
